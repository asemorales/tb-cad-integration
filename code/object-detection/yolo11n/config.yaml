# Reproducibility config for YOLO11n (object detection, Ultralytics via mlx).
model: yolo11n             # mlx --model identifier; resolves to yolo11.yaml at nano scale
mode: object_detection
dataset: ../../../dataset/object-detection/dataset.yaml
epochs: 100                   # matches the committed yolo26/draxnet-yolo26 runs
batch_size: 16               # matches the committed yolo26/draxnet-yolo26 runs
image_size: 256              # M2 fix: matched to yolo26/draxnet-yolo26 (256, not 512)
device: cuda
seed: 0                       # M2 fix: matched to yolo26/draxnet-yolo26 (seed 0)
checkpoint: best-validation   # ultralytics best.pt (--use-best default)
